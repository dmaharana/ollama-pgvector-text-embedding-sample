### Create a local docker instance of postgreSQL with pgvector

`docker compose  up -d`.

### Note update [docker-compose.yaml](./docker-compose.yaml) file as required

- port number
- database storage location
